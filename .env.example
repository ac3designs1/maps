# Google Cloud — enable Places API (New) + Geocoding API, restrict key to your domain
GOOGLE_MAPS_API_KEY=

# Admin dashboard password — access at /admin.html
# Default is "trips-admin" (change this!)
ADMIN_PASS=trips-admin
